# Shuttle Main
# by: KubbyDev

# Interactions with the pilot (The pilot gets the cooldown, the shuttle gets the inputs)
execute as @e[tag=Shuttle] run function tria:weapons/air_vehicle/shuttle/get_pilot
